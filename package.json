{
  "name": "@yysy/lerna",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "boot": "lerna init && lerna bootstrap",
    "dev": "echo dev",
    "version": "lerna version"
  }
}
